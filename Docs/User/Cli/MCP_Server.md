# MCP Server CLI

本文说明本地 MCP server 启动命令。

## 命令

| 命令 | 作用 |
| --- | --- |
| `notion-mcp mcp serve` | 启动本地 MCP server，供 MCP client 连接。 |

## 示例

```bash
notion-mcp mcp serve --transport stdio
```

MCP client 配置见 [MCP Clients](../MCP_Clients.md)。
